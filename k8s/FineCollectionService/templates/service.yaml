apiVersion: v1
kind: Service
metadata:
  name: {{ include "VehicleRegistrationService.fullname" . }}
  labels:
    {{- include "VehicleRegistrationService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "VehicleRegistrationService.selectorLabels" . | nindent 4 }}
