apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "VehicleRegistrationService.fullname" . }}-test-connection"
  labels:
    {{- include "VehicleRegistrationService.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "VehicleRegistrationService.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
